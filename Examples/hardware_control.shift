// Swift-compatible declaration style
var statusLed: UInt32 = 0x40001000
var loopCount: UInt32 = 0

// Custom compiler directives using standard Swift attribute syntax
@attached(peer)
var isVolatile: Bool = true

func executeHardwareControl() {
    statusLed = statusLed | 1
    loopCount += 1
}
