var {
    @name(status_led)
    @type(UInt32)
    @address(0x40001000)
    volatile
    mutating

    @name(loop_count)
    @type(UInt32)
    @value(0)
    mutating
}

status_led = status_led | 1
loop_count = loop_count + 1

asm {
    @target(arm64)
    @instruction("isb")
}
