create.var {
    .name(status_led)
    .type(u32)
    .address(0x40001000)
    .volatile;
    .mutable;

    .name(loop_count)
    .type(u32)
    .value = 0
    .mutable:
}

status_led = status_led | 1
loop_count = loop_count + 1

create.asm {
    .target(arm64)
    .instruction("isb");
}
